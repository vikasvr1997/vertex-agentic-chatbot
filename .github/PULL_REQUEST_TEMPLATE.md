## Summary

<!-- What does this change do, and why? -->

## Test plan

- [ ] `pytest` passes locally
- [ ] `ruff check .` and `mypy src` pass locally
- [ ] Manually exercised the affected frontend(s): Streamlit / Chainlit
- [ ] Updated `openapi/openapi.yaml` if API routes changed (`python scripts/export_openapi.py`)

## Security considerations

<!-- Any change to auth, ADC usage, secrets handling, or the API surface? -->
