# Agentic Chatbot

Ask a question below. This UI talks to the same authenticated backend as
the Streamlit frontend, which connects to Vertex AI via Application
Default Credentials — no API keys involved.
